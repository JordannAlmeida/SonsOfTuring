# Aplication database dependencies

Use the docker compose inside of this folder to run the application database dependencies locally.

```bash
docker-compose up -d
```

# Observability dependencies

You need use langfuse self-hosted observability stack to run the application locally. Follow the instructions in the [langfuse self-hosted repository](https://langfuse.com/self-hosting/deployment/docker-compose) to set it up.